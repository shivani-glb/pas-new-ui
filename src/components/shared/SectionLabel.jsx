import React from 'react';

const SectionLabel = ({ label }) => (
    <div className="px-3 pt-4 pb-2 text-[10px] font-bold text-[#ccc] uppercase tracking-wider">{label}</div>
);

export default SectionLabel;
